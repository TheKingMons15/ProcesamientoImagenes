
export interface Building {
    id: string;
    name: string;
    description: string;
    groupId: number;
    points: any[]; // Si tienes un tipo específico para los puntos, reemplaza `any[]` con ese tipo
  }
  